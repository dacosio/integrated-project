import React from "react";
import "./image.css";

/**
 * Primary UI component for user interaction
 */
export const image = ({ source, size, ...props }) => {

    const style = {

    }
  
  
    return (
    <img src={source}>
    
    </img>
  );
};
