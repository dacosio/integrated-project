import React from "react";
import "./imageLabel.css";

/**
 * Primary UI component for user interaction
 */
export const imageLabel = ({ primary, backgroundColor, color, size, ...props }) => {

    const style = {

    }
  
  
    return (
    <img src={source}>
    
    </img>
  );
};